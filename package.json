{
  "name": "giftplatform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd platform && npm install",
    "build": "cd platform && npm run build",
    "dev": "cd platform && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
