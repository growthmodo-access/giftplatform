{
  "name": "giftplatform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd platform && npm install && npm run build",
    "dev": "cd platform && npm run dev"
  },
  "dependencies": {
    "next": "^15.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
